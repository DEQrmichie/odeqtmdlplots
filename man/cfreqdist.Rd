% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cumulative_frequency.R
\name{cfreqdist}
\alias{cfreqdist}
\title{cfreqdist}
\usage{
cfreqdist(df, station1 = NULL, station2 = NULL,
  station_col = "MLocID", date_col = "SampleStartDate",
  result_col = "Result_Numeric", p1title = NULL, p2title = NULL,
  leg_x_nudge = 0, leg_y_nudge = 0, cfd_diff_x_int = 0)
}
\arguments{
\item{df}{Dataframe of data. Should be in long format}

\item{station1}{Name of station 1 in the station column (station_col)}

\item{station2}{Name of station 2 in the station column (station_col)}

\item{station_col}{Name of the column conatining the station names}

\item{date_col}{Name of the column containing the date information}

\item{result_col}{Name of the column containing the results to plot}

\item{p1title}{Title of the cumulative frequency distribution plot}

\item{p2title}{Title of the difference cumulative frequency distribution plot}

\item{leg_x_nudge}{Move the legend left or right. (-0.25 to 0.25)}

\item{leg_y_nudge}{Move the legend up or down. (-0.25 to 0.25)}
}
\value{
plot of all cumulative frequency and cumulative frequency
 difference
 \if{html}{\figure{cumfreq.png}{Plot}}
 \if{latex}{\figure{cumfreq.png}{options: width=0.5in}}
}
\description{
This function creates a graphc of two plots First plot is the
cumulative frequency distribution of two monitoring locations.
The second is the cumulative frequency of the difference between the
two monitoring locations (station1 - station2). Data input should be a
dataframe of data in 'long' format for two locations. Dataframe should have
at least 3 columns, station, data, and result.
}
\examples{
 \dontrun{
 library(tidyverse)
 library(AWQMSdata)
 data <- AWQMS_Data(station = c('14046778', '14034470'),
 char = 'Temperature, water',
 stat_base = '7DADM',
 crit_codes = TRUE)


 data <- data \%>\%
 mutate(stationname = ifelse(MLocID == "14046778", "without dams",
                      ifelse(MLocID == "14034470", "with dams", "ERROR" ) )) \%>\%
 filter(lubridate::month(SampleStartDate) \%in\% c(7,8))




 cfreqdist(df = data,
           station1 = "without dams",
           station2 = "with dams",
           station_col = "stationname",
           date_col = "SampleStartDate",
           result_col = "Result_Numeric",
           p1title = "July - August",
           p2title = "Difference")
  }
}
